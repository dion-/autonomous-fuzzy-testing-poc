{
  "name": "fuzzy-testing-poc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "oxlint",
    "lint:ci": "oxlint --format=github --deny-warnings",
    "typecheck": "tsc --noEmit",
    "test:bombadil": "pnpm exec vite preview --port 4173 --host & npx wait-on http://localhost:4173 && bombadil test http://localhost:4173 --no-sandbox --exit-on-violation --time-limit 2m --output-path ./bombadil-results",
    "gh-workflow": "AI_AGENT=1 npx @redwoodjs/agent-ci run --workflow .github/workflows/fuzzy-test.yml --var BOMBADIL_TIME_LIMIT=2m",
    "gh-workflow:fast": "AI_AGENT=1 npx @redwoodjs/agent-ci run --workflow .github/workflows/fuzzy-test.yml --var BOMBADIL_TIME_LIMIT=30s",
    "gh-workflow:pause": "AI_AGENT=1 npx @redwoodjs/agent-ci run --workflow .github/workflows/fuzzy-test.yml --var BOMBADIL_TIME_LIMIT=2m --pause-on-failure",
    "ci:local": "npx @redwoodjs/agent-ci run --all --var BOMBADIL_TIME_LIMIT=2m"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "cpu-features",
      "protobufjs",
      "ssh2"
    ]
  },
  "devDependencies": {
    "@antithesishq/bombadil": "^0.4.5",
    "@tailwindcss/vite": "^4.2.4",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "oxlint": "^1.62.0",
    "tailwindcss": "^4.2.4",
    "typescript": "~6.0.2",
    "vite": "^8.0.10",
    "vitest": "^4.1.5",
    "wait-on": "^9.0.5",
    "@redwoodjs/agent-ci": "^0.15.0",
    "@types/node": "^22.0.0"
  }
}
